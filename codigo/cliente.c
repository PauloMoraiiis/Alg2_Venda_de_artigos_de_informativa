#include <stdio.h>
#include <string.h>

#define MAX 5

// implementar: ESTRUTURA 3 - VENDA (typedef struct, igual ao main.c)

// implementar: ESTRUTURA 1 - CLIENTE (typedef struct, igual ao main.c)

// implementar: buscarCliente - recebe o vetor, o total e o codigo, retorna a posicao ou -1

// implementar: salvarClientes - salva o vetor em clientes.bin usando fwrite

// implementar: carregarClientes - le o vetor de clientes.bin usando fread, retorna o total

// implementar: cadastrarCliente - pede os dados, verifica se codigo ja existe, insere no vetor

// implementar: consultarCliente - pede o codigo, busca e mostra os dados

// implementar: alterarCliente - pede o codigo, busca e deixa alterar os campos

// implementar: removerCliente - pede o codigo, verifica se tem venda, remove deslocando o vetor

// implementar: listarClientes - percorre o vetor e mostra todos os clientes

// implementar: MENU CLIENTE (loop com switch chamando as funcoes acima)
